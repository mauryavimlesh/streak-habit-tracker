# Environment Variables
# The app now uses an in-app Configuration Dashboard to manage these values without blocking startup.
# Production deployments should still provide GEMINI_API_KEY via the environment.
